# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    page:
        label: 'catdesign.seocore::lang.models.meta_page.columns.page.label'
        searchable: true
        select: "concat(page_id, ' | ', page_title)"
    name:
        label: 'catdesign.seocore::lang.models.meta_page.columns.name.label'
        searchable: true

